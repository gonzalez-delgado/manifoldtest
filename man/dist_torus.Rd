% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifold-torus.R
\name{dist_torus}
\alias{dist_torus}
\title{Torus distance from a data set to a point}
\usage{
dist_torus(X, p)
}
\arguments{
\item{X}{A matrix or data frame of torus data in \eqn{[0,1)^d}; rows are
samples, columns are coordinates.}

\item{p}{Numeric vector of length \code{ncol(X)}: a point on the torus.}
}
\value{
Numeric vector of length \code{nrow(X)}: the torus distance from
  each row of \code{X} to \code{p}.
}
\description{
Computes the flat-torus distance between each row of \code{X} and the
point \code{p}: for each coordinate, the shortest wraparound distance
\code{min(|x-p|, 1-|x-p|)} on the unit circle, combined across
coordinates via the usual Euclidean norm.
}
\examples{
X <- matrix(runif(20), nrow = 10, ncol = 2)
dist_torus(X, c(0.5, 0.5))

}
