% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{align_sample_sizes}
\alias{align_sample_sizes}
\title{Align sample sizes of two data sets}
\usage{
align_sample_sizes(X_raw, Y_raw, seed = 2024)
}
\arguments{
\item{X_raw}{A matrix or data frame; rows are samples.}

\item{Y_raw}{A matrix or data frame; rows are samples.}

\item{seed}{Integer random seed used when subsampling, for reproducibility.
Defaults to \code{2024}.}
}
\value{
A list with two elements, \code{X} and \code{Y}, each a matrix or
  data frame with matching row counts. If the inputs already had equal
  row counts, they are returned unchanged.
}
\description{
Checks whether \code{X_raw} and \code{Y_raw} have the same number of rows,
and if not, randomly subsamples both to the smaller of the two sizes so
that they can be compared or paired downstream.
}
\examples{
X_raw <- matrix(rnorm(30), nrow = 10)
Y_raw <- matrix(rnorm(24), nrow = 8)
aligned <- align_sample_sizes(X_raw, Y_raw)
nrow(aligned$X)
nrow(aligned$Y)

}
